import sinon from 'sinon';
import {expect} from 'chai';
import {OrganisationController} from '../../../../src/server/areas/organisation/organisation_controller';
/** @test {OrganisationController} */
describe('OrganisationController', () => {
  let controller;
  before(() => {
    controller = new OrganisationController();
  });
  /** @test {OrganisationController#fetch} */
  describe('#fetch', () => {});
});
